services:
  backend:
    container_name: port26-backend
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      APP_ENV: production
    ports:
      - "8000:8000"
    restart: unless-stopped
